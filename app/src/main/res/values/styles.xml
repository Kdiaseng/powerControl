<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorGreenAccent</item>
        <item name="colorPrimaryDark">@color/colorBlackLight</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CardItemData" parent="CardView">
        <item name="cardBackgroundColor">@color/colorBlackLight</item>
        <item name="cardCornerRadius">5dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">5dp</item>
    </style>
    <style name="LinearItemData">
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">42dp</item>
    </style>


    <style name="Widget.MaterialComponents.TextInputLayout.OutlinedBox.MyTextInput">
        <item name="boxStrokeColor">@color/colorGreenAccent</item>
        <item name="errorEnabled">true</item>
        <item name="hintTextColor">@color/colorGreenAccent</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColorHint">@color/colorGreenAccent</item>
    </style>

    <style name="StyleEditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorGreenAccent</item>
    </style>

</resources>
